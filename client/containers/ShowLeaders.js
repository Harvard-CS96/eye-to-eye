import { connect } from 'react-redux';
import _ShowLeaders from '../components/ShowLeaders';


const mapStateToProps = (state, ownProps) => ({
})


const mapDispatchToProps = {

} 

const ShowLeaders = connect(mapStateToProps, mapDispatchToProps)(_ShowLeaders);
export default ShowLeaders;