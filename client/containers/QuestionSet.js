import { connect } from 'react-redux';
import _QuestionSet from '../components/QuestionSet';


const mapStateToProps = (state, ownProps) => ({
})


const mapDispatchToProps = {

} 

const QuestionSet = connect(mapStateToProps, mapDispatchToProps)(_QuestionSet);
export default QuestionSet;