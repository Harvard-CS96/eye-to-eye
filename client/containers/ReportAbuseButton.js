import { connect } from 'react-redux';
import _ReportAbuseButton from '../components/ReportAbuseButton';


const mapStateToProps = (state, ownProps) => ({

})


const mapDispatchToProps = {
} 

const ReportAbuseButton = connect(mapStateToProps, mapDispatchToProps)(_ReportAbuseButton);
export default ReportAbuseButton;