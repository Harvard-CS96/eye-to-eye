import { connect } from 'react-redux';
import _HideLeaders from '../components/HideLeaders';


const mapStateToProps = (state, ownProps) => ({
})


const mapDispatchToProps = {

} 

const HideLeaders = connect(mapStateToProps, mapDispatchToProps)(_HideLeaders);
export default HideLeaders;